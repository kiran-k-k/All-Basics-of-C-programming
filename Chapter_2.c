// Instructions & Operator

/*
//Write a program to check anumbner is devisible by 2 or not.
#include<stdio.h>
int main(){
    int num;

    printf("enter number :");
    scanf("%d", &num);

    printf("if it returns 1 then number is devisible by 2 \n");
    printf("if it returns 0 then number is not devisible by 2 \n");

    printf("it returns : %d \n", num % 2 == 0);

    return 0;
    
}
*/

/*
//Write a program to print the average of 3 numbers.
#include<stdio.h>
int main()
{
    int num_1, num_2, num_3, average;

    printf("enter numbers :");
    scanf("%d %d %d", &num_1, &num_2, &num_3);

    average = (num_1 + num_2 + num_3) / 3;

    printf("Avearge of %d, %d, & %d is : %d \n", num_1, num_2, num_3, average);

    return 0;
}
*/

/*
//Write a program to check if given character is digitor not.
#include<stdio.h>
int main()
{
    char character;

    printf("enter cahracter :");
    scanf("%c", &character);

    printf("if it raeurns 1 then it is digit \n");
    printf("if it returns 0 then it is character \n");

    printf("it returns %d \n", ('0' <= character && character <= '9'));

    return 0;
}
*/

/*
// Write a program to print the smallest number of two.
#include<stdio.h>
int main()
{
    int num_1, num_2;

    printf("enter numbers :");
    scanf("%d %d", &num_1, &num_2);

    printf("smallest number is : %d \n", ((num_1 < num_2) ? num_1  : num_2 ));

    return 0;
}
*/