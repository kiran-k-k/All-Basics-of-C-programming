//Variables, Data types, Input / Output

/*
// erite a C program that calcularte the area of a square
#include <stdio.h>
int main(){
    
    float side, area;
    printf("enter side of square :");
    scanf("%f", &side);

    area = side * side;

    printf("area of square is : %.2f ", area);
    
    return 0;
} 
*/

/*
//Write a program to calculate area of a circle 
#include<stdio.h>
int main()
{
    float radious, area;

    printf("enter radious of circle :");
    scanf("%f", &radious);

    area = 3.14 * radious * radious;
    printf("radious of circle is : %.2f ", area);

    return 0;
}
*/

/*
//Write a program to calculate perimeter of rectangle. Take sides, a & b, from the user.
#include<stdio.h>
int main()
{
    float a, b, parameter;
    printf("enter value of a & b :");
    scanf("%f %f", &a, &b);

    parameter = 2 * (a + b);
    
    printf("parameter of rectangle is : %.2f", parameter);

    return 0;
}
*/

/*
//Take a number(n) from user & output its cube(n*n*n).
#include<stdio.h>
#include<math.h>
int main()
{
    int num, cube;

    printf("enter a number :");
    scanf("%d", &num);

    cube = pow(num, 3);

    printf("cube of %d is : %d ", num, cube);

    return 0;
}
*/




